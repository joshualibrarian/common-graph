package dev.everydaythings.graph.network.transport;

/**
 * Network transport interface.
 * Stub - full implementation TBD.
 */
public interface Transport {
    // TODO: Define transport methods once network types are implemented
    // CompletableFuture<PeerLink> connect(PeerAddress addr, TransportOptions opts);
    // void listen(PeerBind bind, IncomingSessionHandler handler);
}
