// =============================================================================
// :media — Media items (photographs, video, audio, documents)
//
// Content-bearing items with structured metadata.
// Depends on :core for Item infrastructure.
// =============================================================================

plugins {
    id 'java-library'
}

dependencies {

    // Core module (Item, Library, etc.)
    api project(':core')
}
